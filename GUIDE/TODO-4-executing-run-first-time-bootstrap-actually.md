# Running TODO — 4) Run first-time bootstrap (work log)

## Update log (append-only)
- [x] Run `python main.py`

- [ ] Confirm DB + tables created
- [ ] Confirm admin seed exists (admin/admin123)
- [ ] Confirm initial `skills` and `projects` seed inserted when empty

## Acceptance criteria
- App does not exit due to schema errors.
- Menu navigation works after bootstrap.

