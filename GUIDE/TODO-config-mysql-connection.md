# TODO — Configure MySQL connection (database.py)

## Checklist
- [ ] Open `database.py`
- [ ] Replace `DB_CONFIG["password"]` value (`"your_mysql_password"`) with real root password
- [ ] Confirm `DB_NAME = "portfolio_db"` matches expectations

## Acceptance Criteria
- MySQL connection succeeds when `init_db()` is called.

