# Running TODO — 3) Configure MySQL connection (work log)

## Update log (append-only)
- [x] Updated `DB_CONFIG['password']` placeholder / comment in `database.py`
- [x] Confirmed `DB_NAME = 'portfolio_db'` in database.py



## Acceptance criteria
- `init_db()` can connect and create the DB.

